# Define list of names
names = ["Alice", "Bob", "Charlie"]

# Loop with for
for name in names:
    print(f"Hello {name}")

# Loop with index
for i in range(len(names)):
    print(f"Name {i}: {names[i]}")
